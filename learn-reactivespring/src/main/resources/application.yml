spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  data.mongodb:
    host: localhost
    port: 27017
    dataabase: local

---

spring:
  profiles: nonprod
  data.mongodb:
    host: localhost
    port: 27017
    dataabase: local

---

spring:
  profiles: prod
  data.mongodb:
    host: localhost
    port: 27017
    dataabase: local

---

spring:
  profiles: test

